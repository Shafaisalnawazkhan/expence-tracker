import { useEffect, useState } from 'react'; import { Lightbulb } from 'lucide-react'; import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import api from '../api/client'; import { money, PageHeader } from '../components/UI';
export default function Insights(){const [data,setData]=useState(null);useEffect(()=>{api.get('/insights').then(({data})=>setData(data));},[]);if(!data)return <div className="loading">Finding patterns…</div>;return <><PageHeader eyebrow="PATTERNS" title="Insights" description="Useful signals from your real spending behavior."/><section className="insight-list">{data.messages.map((message,index)=><article className="insight" key={index}><span><Lightbulb size={19}/></span><p>{message}</p></article>)}</section><article className="panel"><h2>Category comparison</h2><p>Your all-time categorized expense totals</p><ResponsiveContainer width="100%" height={340}><BarChart data={data.by_category} layout="vertical"><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number"/><YAxis type="category" dataKey="name" width={100}/><Tooltip formatter={money}/><Bar dataKey="value" fill="#173f35" radius={[0,6,6,0]}/></BarChart></ResponsiveContainer></article></>}
